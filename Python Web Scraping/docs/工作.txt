
1. 根据内容检索HTML文件：
    用BS4库先将HTML文件转化成一颗多叉树
    用深度优先搜索搜索树内包含有该内容的结点
    用一个列表装填这些结点

2. 通过BS4的库函数得到列表中每一个元素的祖先结点
    从底层搜索每一个祖先结点
    返回第一个含有class标签的祖先结点

3. 将祖先结点转化为json数据并进行去重
    将祖先结点的标签名和标签的属性提取出

4. 用BS4的库函数得到所有类似拥有标签的结点
    严格地匹配标签的属性，选出属于一类标签的结点

5. 输出文件